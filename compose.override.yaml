---
# Development environment override
services:
  php:
    build:
      context: .
      target: frankenphp_dev
    volumes:
      - ./:/app
      - ./frankenphp/Caddyfile:/etc/frankenphp/Caddyfile:ro
      - ./frankenphp/conf.d/20-app.dev.ini:/usr/local/etc/php/app.conf.d/20-app.dev.ini:ro
      # If you develop on Mac or Windows you can remove the vendor/ directory
      #  from the bind-mount for better performance by enabling the next line:
      - /app/vendor
      # Persist encryption key across container rebuilds (VOLUME /app/var/ in Dockerfile
      # creates an anonymous volume that is lost on rebuild)
      - encryption-key:/app/var/encryption
    environment:
      # `:80` is an HTTP catch-all block (any Host header) so traffic forwarded by
      # ngrok (`ngrok http 80`) matches without needing the public hostname here.
      # The HTTPS hosts (localhost / 192.168.137.1 hotspot / 192.168.20.119 Wi-Fi)
      # are kept for direct LAN access.
      SERVER_NAME: "${SERVER_NAME:-localhost, 192.168.137.1, 192.168.20.119, php:80, :80}"
      FRANKENPHP_WORKER_CONFIG: watch
      MERCURE_EXTRA_DIRECTIVES: demo
      # See https://xdebug.org/docs/all_settings#mode
      XDEBUG_MODE: "${XDEBUG_MODE:-off}"
      APP_ENV: "${APP_ENV:-dev}"
      # Trust ngrok / docker-network reverse proxies so Symfony sees the original
      # client IP and detects HTTPS via X-Forwarded-Proto.
      SYMFONY_TRUSTED_PROXIES: "127.0.0.1,REMOTE_ADDR,private_ranges"
      SYMFONY_TRUSTED_HEADERS: "x-forwarded-for,x-forwarded-host,x-forwarded-proto,x-forwarded-port,x-forwarded-prefix"
    extra_hosts:
      # Ensure that host.docker.internal is correctly defined on Linux
      - host.docker.internal:host-gateway
    tty: true

  # Flutter mobile APK — download page at http://localhost:3000 or http://192.168.137.1:3000
  mobile:
    build:
      context: ../greffe_renale_mobile
      args:
        API_BASE_URL: https://192.168.137.1
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - php
###> symfony/mercure-bundle ###
###< symfony/mercure-bundle ###

###> doctrine/doctrine-bundle ###
  database:
    ports:
      - "5432"
###< doctrine/doctrine-bundle ###

volumes:
  encryption-key:

