<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Gestion des Greffes Rénales{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🫘</text></svg>">
        <link rel="stylesheet" href="/css/style.css">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <header>
            <div class="header-container">
                <a href="{{ path('app_home') }}" class="header-logo">
                    Greffes Rénales
                </a>

                {% if app.user %}
                <nav class="header-nav">
                    {% if is_granted('ROLE_DOCTOR') or is_granted('ROLE_NURSE') %}
                    <a href="{{ path('app_patient_index') }}" class="header-link">Patients</a>
                    {% endif %}
                    {% if is_granted('ROLE_DOCTOR') or is_granted('ROLE_NURSE') or is_granted('ROLE_TRANSPLANT_COORDINATOR') %}
                    <a href="{{ path('app_donor_index') }}" class="header-link">Donneurs</a>
                    {% endif %}
                    {% if is_granted('ROLE_TECH_ADMIN') %}
                    <a href="{{ path('app_admin_users') }}" class="header-link header-admin">Administration</a>
                    <a href="{{ path('app_admin_audit_logs') }}" class="header-link header-admin">Journal d'audit</a>
                    {% endif %}
                    <a href="{{ path('app_profile') }}" class="header-link header-profile">{{ app.user.fullName }}</a>
                    <a href="{{ path('app_logout') }}" class="header-link">Déconnexion</a>
                </nav>
                {% endif %}
            </div>
            {% block header %}{% endblock %}
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
