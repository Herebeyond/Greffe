---
# Production environment override
services:
  php:
    build:
      context: .
      target: frankenphp_prod
    environment:
      CADDY_GLOBAL_OPTIONS: ${CADDY_GLOBAL_OPTIONS:-}
      SERVER_NAME: ${SERVER_NAME:-localhost}
      APP_SECRET: ${APP_SECRET}
      ENCRYPTION_KEY_PATH: /app/var/encryption/encryption.key
      MERCURE_PUBLISHER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET}
      MERCURE_SUBSCRIBER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET}
      CADDY_SERVER_EXTRA_DIRECTIVES: ${CADDY_SERVER_EXTRA_DIRECTIVES:-}
    volumes:
      - ./frankenphp/certs:/etc/caddy/certs:ro
      - encryption-key:/app/var/encryption

  mobile:
    build:
      context: ../greffe_renale_mobile
      args:
        API_BASE_URL: ${MOBILE_API_BASE_URL:-https://10.187.22.27}
    restart: unless-stopped
    ports:
      - "${MOBILE_PORT:-3000}:80"
    depends_on:
      - php

volumes:
  encryption-key:
